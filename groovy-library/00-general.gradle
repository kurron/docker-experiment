logger.quiet '00-general.gradle'

buildscript {
    allprojects {
       apply plugin: 'project-report'
       apply plugin: 'build-dashboard'

       defaultTasks << 'projectReport'
       defaultTasks << 'buildDashboard'
    }
}
