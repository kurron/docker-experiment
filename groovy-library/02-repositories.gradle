logger.quiet '02-repositories.gradle'

buildscript {
    allprojects {
        repositories {
            jcenter()
        }      

        configurations.all {
            exclude group: 'commons-logging'
            exclude module: 'junit-dep'
        }
    }
}
